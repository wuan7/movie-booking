/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['deafening-retriever-318.convex.cloud', 'github.com', 'movie-booking-six.vercel'],
      },
};

export default nextConfig;
