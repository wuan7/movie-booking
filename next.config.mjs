/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['deafening-retriever-318.convex.cloud', 'github.com', 'movie-booking-six.vercel', 'cool-turtle-145.convex.cloud'],
      },
};

export default nextConfig;
